# Assets / images

Drop your app icon, splash, and other image assets here.

Once you add an `icon.png` and `splash-icon.png`, reference them back in
`app.json` (under `expo.icon` and the `expo-splash-screen` plugin) the same way
the OLD APP did. The scaffold ships without custom icons so it runs out of the
box with Expo's defaults.

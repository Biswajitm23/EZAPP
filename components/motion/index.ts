/**
 * Motion primitives — the app's shared animation vocabulary.
 *   import { PressableScale, Reveal } from '@/components/motion'
 */
export { PressableScale } from './PressableScale'
export { Reveal } from './Reveal'
